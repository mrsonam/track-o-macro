/** Meals per page on `/history` and `GET /api/meals/history`. */
export const HISTORY_MEALS_PAGE_SIZE = 120;

/** Upper bound for `offset` to keep history browsing bounded. */
export const HISTORY_MEALS_MAX_OFFSET = 36_000;
