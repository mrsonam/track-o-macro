/** @deprecated Import from `@/lib/motion` — kept for landing imports. */
export {
  DURATION,
  EASE_OUT as LANDING_EASE_OUT,
  STAGGER,
  revealItem as landingRevealItem,
  revealStaggerItem as landingRevealStaggerItem,
  Reveal,
  RevealItem,
  RevealLine,
  RevealStagger,
  useAppMotion as useLandingMotion,
  useMotionInView,
  useRevealLatch,
} from "@/lib/motion";
